name = "sunny"
print(len(name)) #length of string

print(name[0:3]) #slicing of string
print(name[0:5:2]) #slicing of string with step

#functions of string
#capitalize, upper, lower, title, count, find, replace, center, ljust, rjust, strip, lstrip, rstrip, split, join